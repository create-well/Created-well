#!/usr/bin/env bash
set -euo pipefail
npm run build
[[ -d "dist" ]] || {
  echo "Expected build output at dist" >&2
  exit 1
}
if command -v figma &> /dev/null; then
  figma make deploy --build-dir "dist"
else
  echo "Build verified. Ready for Figma Make deployment."
fi
